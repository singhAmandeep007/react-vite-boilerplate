# crvb

Scaffold the React Vite Boilerplate with one command.

## Usage

```sh
npx crvb@latest my-app
```

## What it does

- Clones the latest template from GitHub
- Removes template release/CLI-only files
- Sets the generated project's package name to your folder name
- Optionally initializes Git
- Optionally installs dependencies
